rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TESTING RULES - VERY PERMISSIVE!
    // Use this ONLY for testing, then switch to firestore.rules
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
