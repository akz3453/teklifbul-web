export default function Dashboard() {
  return (
    <div style={{ padding: 20, maxWidth: 1200, margin: "0 auto" }}>
      <h1>Dashboard</h1>
      <p>React Dashboard component - this is a placeholder.</p>
      <p>
        The main application uses HTML files for the dashboard functionality.
        This React component is kept for compatibility with the Vite setup.
      </p>
    </div>
  );
}
