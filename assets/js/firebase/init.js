/**
 * Firebase Initialization Module
 * Centralized Firebase configuration and initialization
 * Teklifbul Rule v1.0 - Root firebase.js'e re-export yapar (duplicate app hatası önleme)
 */

// Root firebase.js'den tüm export'ları re-export et
export * from '../../../firebase.js';
