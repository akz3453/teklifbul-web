export async function executeRecaptchaV3(_action: string): Promise<string | null> {
  // eslint-disable-next-line no-console -- legacy shim, logger henüz mevcut olmayabilir
  console.warn("[reCAPTCHA] executeRecaptchaV3 kaldırıldı. App Check koruması devrede.");
  return null;
}

